{
  db {
    uri = "s3://"
    version = "v1"
  }
  geneAnnotations = "s3://..."
  storageLevel = "MEMORY_ONLY_1"
  partitions = 1
  geneFeatures {
    probesetID = probesetid,
    dataType = dataType,
    ENTREZID = entrezid,
    ENSEMBL = ensemblid,
    SYMBOL = symbol,
    GENENAME = name,
    GENEFAMILY = geneFamily
  }
}
